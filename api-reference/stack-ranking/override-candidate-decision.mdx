---
title: "Override Candidate Decision"
api: "POST /graphql"
description: "Override the candidate-level (across-rounds) AI verdict for a candidate on a job"
---

## Overview

Override the candidate-level AI verdict — the single across-rounds decision recorded once per candidate per job. The semantics are the same as `overrideRoundDecision` but for candidate-type decision rows: the mutation patches the `override` block on the existing row and never touches the original `aiVerdict`. Read candidate-level verdicts back with `leaderboardCandidateDecisions`, and review override history with `leaderboardAuditTrail`.

<Warning>
This operation requires the `decisions:write` scope and mutates the hiring decision audit trail. It is never available in the default read tier.
</Warning>

## Endpoint

```
POST /graphql
Content-Type: application/json
```

## GraphQL Mutation

```graphql
mutation overrideCandidateDecision($input: OverrideDecisionInput!) {
  overrideCandidateDecision(input: $input) {
    decisionId
    candidateId
    candidateName
    jobId
    aiVerdict
    aiScore
    aiReasoning
    aiProducedAt
    override {
      verdict
      reason
      byUserId
      at
    }
    effectiveVerdict
  }
}
```

## Request Parameters

<ParamField body="decisionId" type="string" required>
  ID of the candidate-level AI decision row to override. If no AI decision exists at this ID, the request fails with `BAD_USER_INPUT` (`NO_AI_DECISION_TO_OVERRIDE`).
</ParamField>

<ParamField body="verdict" type="AiDecisionVerdict" required>
  The verdict to record as the override. Allowed values: `ADVANCE`, `HOLD`, `REJECT`.
</ParamField>

<ParamField body="reason" type="string" required>
  Justification for the override, recorded in the audit trail. Must be non-empty.
</ParamField>

<ParamField body="expectedVersion" type="integer">
  Optional optimistic-concurrency guard. If supplied and stale, the request is rejected with a version-conflict error instead of silently overwriting a newer override.
</ParamField>

## Request Example

<CodeGroup>

```bash cURL
curl -X POST https://api.exterview.ai/graphql \
  -H "x-api-key: YOUR_API_KEY" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "mutation overrideCandidateDecision($input: OverrideDecisionInput!) { overrideCandidateDecision(input: $input) { decisionId candidateId candidateName jobId aiVerdict aiScore aiReasoning aiProducedAt override { verdict reason byUserId at } effectiveVerdict } }",
    "variables": {
      "input": {
        "decisionId": "dec_cand_5b8e1f3a7c2d",
        "verdict": "HOLD",
        "reason": "Waiting on a second panel opinion before rejecting; scores are borderline.",
        "expectedVersion": 1
      }
    }
  }'
```

```javascript JavaScript
const query = `
  mutation overrideCandidateDecision($input: OverrideDecisionInput!) {
    overrideCandidateDecision(input: $input) {
      decisionId
      candidateId
      candidateName
      jobId
      aiVerdict
      aiScore
      aiReasoning
      aiProducedAt
      override {
        verdict
        reason
        byUserId
        at
      }
      effectiveVerdict
    }
  }
`;

const variables = {
  input: {
    decisionId: "dec_cand_5b8e1f3a7c2d",
    verdict: "HOLD",
    reason: "Waiting on a second panel opinion before rejecting; scores are borderline.",
    expectedVersion: 1
  }
};

const response = await fetch('https://api.exterview.ai/graphql', {
  method: 'POST',
  headers: {
    'x-api-key': 'YOUR_API_KEY',
    'Authorization': 'Bearer YOUR_TOKEN',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ query, variables })
});

const data = await response.json();
```

```python Python
import requests

url = "https://api.exterview.ai/graphql"
headers = {
    "x-api-key": "YOUR_API_KEY",
    "Authorization": "Bearer YOUR_TOKEN",
    "Content-Type": "application/json"
}

query = """
mutation overrideCandidateDecision($input: OverrideDecisionInput!) {
  overrideCandidateDecision(input: $input) {
    decisionId
    candidateId
    candidateName
    jobId
    aiVerdict
    aiScore
    aiReasoning
    aiProducedAt
    override {
      verdict
      reason
      byUserId
      at
    }
    effectiveVerdict
  }
}
"""

variables = {
    "input": {
        "decisionId": "dec_cand_5b8e1f3a7c2d",
        "verdict": "HOLD",
        "reason": "Waiting on a second panel opinion before rejecting; scores are borderline.",
        "expectedVersion": 1
    }
}

response = requests.post(url, headers=headers, json={"query": query, "variables": variables})
print(response.json())
```

</CodeGroup>

## Response

<ResponseField name="data" type="object">
  <Expandable title="data properties">
    <ResponseField name="overrideCandidateDecision" type="object">
      The updated candidate-level AI decision row, including the new override block. One row exists per candidate per job.

      <Expandable title="overrideCandidateDecision properties">
        <ResponseField name="decisionId" type="string">
          Unique ID of the decision row
        </ResponseField>

        <ResponseField name="candidateId" type="string">
          UUID of the candidate the decision applies to
        </ResponseField>

        <ResponseField name="candidateName" type="string">
          Candidate display name, joined from the candidates store at resolver time (nullable)
        </ResponseField>

        <ResponseField name="jobId" type="string">
          UUID of the job the decision belongs to
        </ResponseField>

        <ResponseField name="aiVerdict" type="AiDecisionVerdict">
          The AI's original across-rounds verdict, unchanged by the override. One of `ADVANCE`, `HOLD`, `REJECT`.
        </ResponseField>

        <ResponseField name="aiScore" type="integer">
          Numeric score the AI produced for the candidate
        </ResponseField>

        <ResponseField name="aiReasoning" type="string">
          The AI's explanation for its verdict
        </ResponseField>

        <ResponseField name="aiProducedAt" type="string">
          ISO 8601 timestamp when the AI produced the verdict
        </ResponseField>

        <ResponseField name="override" type="object">
          Recruiter override block (null when the row has never been overridden).

          <Expandable title="override properties">
            <ResponseField name="verdict" type="AiDecisionVerdict">
              The overriding verdict: `ADVANCE`, `HOLD`, or `REJECT`
            </ResponseField>

            <ResponseField name="reason" type="string">
              Justification supplied with the override
            </ResponseField>

            <ResponseField name="byUserId" type="string">
              ID of the user who applied the override
            </ResponseField>

            <ResponseField name="at" type="string">
              ISO 8601 timestamp of the override
            </ResponseField>
          </Expandable>
        </ResponseField>

        <ResponseField name="effectiveVerdict" type="AiDecisionVerdict">
          Derived field resolving to the override's verdict when present, otherwise `aiVerdict`. One of `ADVANCE`, `HOLD`, `REJECT`.
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

## Response Example

<ResponseExample>

```json 200 OK
{
  "data": {
    "overrideCandidateDecision": {
      "decisionId": "dec_cand_5b8e1f3a7c2d",
      "candidateId": "9e2f6b1a-4c8d-4a7e-b3f9-5d0c1e2a3b4c",
      "candidateName": "Daniel Okafor",
      "jobId": "b4d2f8a1-6c3e-4d9b-a7f5-0e1c2b3a4d5f",
      "aiVerdict": "REJECT",
      "aiScore": 48,
      "aiReasoning": "Below-threshold scores in the avatar interview and resume rubric across required competencies.",
      "aiProducedAt": "2026-06-30T11:47:19.000Z",
      "override": {
        "verdict": "HOLD",
        "reason": "Waiting on a second panel opinion before rejecting; scores are borderline.",
        "byUserId": "usr_4d8f2a91c6b3",
        "at": "2026-07-02T15:04:52.000Z"
      },
      "effectiveVerdict": "HOLD"
    }
  }
}
```

```json 401 Unauthorized
{
  "errors": [
    {
      "message": "Unauthenticated",
      "extensions": {
        "code": "UNAUTHENTICATED"
      }
    }
  ]
}
```

</ResponseExample>

## Error Responses

| Error Code | Description |
|------------|-------------|
| UNAUTHENTICATED | Missing or invalid authentication credentials |
| FORBIDDEN | Caller lacks the decisions-override permission |
| BAD_USER_INPUT | No AI decision exists at `decisionId` (`NO_AI_DECISION_TO_OVERRIDE`), the `reason` is empty, or the `verdict` is unknown |
| INTERNAL_SERVER_ERROR | Unexpected server-side failure while writing the override |
